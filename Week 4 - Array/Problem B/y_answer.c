#include <stdio.h>

int main() {
    unsigned short T;
    scanf("%hu", &T);
    unsigned short n, m, q;
    for (unsigned short i = 1; i <= T; i++) {
        printf("Case #%hu: ", i);
        puts(" ");
    }
    return 0;
}